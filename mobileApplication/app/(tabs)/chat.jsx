import React from "react";
import { View, Text, SafeAreaView } from "react-native";

const Chat = () => {
  return (
    <SafeAreaView className="flex-1 bg-white">
      <View className="p-4">
        <Text className="text-2xl">Chat Page</Text>
      </View>
    </SafeAreaView>
  );
};

export default Chat;
